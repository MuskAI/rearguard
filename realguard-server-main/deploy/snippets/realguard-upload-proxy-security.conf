# Upload/model routes are slower and larger, so they get a gentler request-rate guard.
limit_req zone=realguard_upload burst=20 nodelay;
proxy_http_version 1.1;
proxy_set_header Proxy "";
proxy_hide_header X-Powered-By;
